/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [{
            hostname: 'www.creativefabrica.com'
        }]
    },
};

export default nextConfig;
